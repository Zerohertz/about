// Animation constants
export const ANIMATION_DELAYS = {
  TRIGGER_DELAY: 50,
  TRANSITION_DELAY: 50,
} as const;

// Image constants
export const IMAGE_DEFAULTS = {
  DEFAULT_SIZE: 5000,
  PROFILE_SIZE: 300,
  QUALITY: 100,
} as const;

// Component styling constants
export const LINK_CLASSES = {
  DEFAULT: "link-underline link-underline-opacity-0 link-underline-opacity-100-hover",
} as const;
